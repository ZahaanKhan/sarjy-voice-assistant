# Copy this file to .env.local for local development.
# Add your real keys to .env.local only — that file is gitignored and never committed.
# In production, set these in the Vercel dashboard under Environment Variables.

GROQ_API_KEY=your_groq_api_key_here
WEATHER_API_KEY=your_openweathermap_api_key_here
